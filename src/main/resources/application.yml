spring:
  application:
    name: starter
  datasource:
    url: jdbc:mysql://localhost:3306/starterdb
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
# http://localhost:8080/
server:
  port: 8080
# http://localhost:8080/actuator/health
management:
  endpoints:
    web:
      exposure:
        include: health,info
springdoc:
  show-actuator: true