spring:
  application:
    name: starter
  datasource:
    url: jdbc:mysql://localhost:3306/starterdb
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
server:
  port: 8080
